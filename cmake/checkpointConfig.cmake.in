# Get compiler and flags
get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(${SELF_DIR}/checkpointTargets.cmake)

set (darma_detector_DIR @detector_DIR@)
find_package(detector)
